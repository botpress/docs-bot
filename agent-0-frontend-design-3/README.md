# agent-0-frontend (Design 3)

Placeholder folder for the third design direction. Awaiting design specs.

When direction is decided, scaffold:
- Same `@botpress/webchat` connection logic as `agent-0-frontend-copilot/`
- Same `useParentWindowMessages` hook for the docs integration
- Same `CLIENT_ID` (agent-0's webchat config)
- Different visual layer

Conventions for this slot:
- **Vite dev port**: `5175`
- **Vite `base`**: `/docs-bot/agent-0-design-3/`
- **gh-pages deploy path**: `https://jacksonyzj.github.io/docs-bot/agent-0-design-3/`

To preview against the docs site, swap `ADK_BOT_URL` in `docs/assistant.js` to `http://localhost:5175/docs-bot/agent-0-design-3/`.
